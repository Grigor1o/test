git status - статус
git add[files] - добавляет файлы в stage
git commit -m "comment"
git log 
git log --oneline
git push [rep_link] [branch_name]

git push origin master

git reset

git reset --hard - отменяет изменения до прощлого сохранения

git diff

gitignore нужен чтобы игнорийть некотрорые файлы или папки, чтобы не пушить их в гит
